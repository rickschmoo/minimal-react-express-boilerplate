{
  "name": "minimal-react-express-boilerplate",
  "version": "1.0.0",
  "description": "SERVER PACKAGE.JSON",
  "main": "server.js",
  "scripts": {
    "server": "nodemon server.js",
    "client": "cd client && yarn start",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.16.3"
  }
}
